# Порт, на котором запускается Vite dev-сервер
# Должен совпадать с FRONTEND_PORT в корневом .env
FRONTEND_PORT=5173

# Базовый URL бэкенд API
# Все запросы с фронтенда идут через этот адрес
# Должен совпадать с APP_HOST:APP_PORT из backend/.env
VITE_API_URL=http://localhost:7812/api/
