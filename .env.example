# Порт бэкенда — используется для маппинга портов в docker-compose
# Должен совпадать с APP_PORT в backend/.env
APP_PORT=7812

# Порт фронтенда — используется для маппинга портов в docker-compose
# Должен совпадать с FRONTEND_PORT в frontend/.env
FRONTEND_PORT=5173
